databases:
  - name: cash-buddy
    databaseName: cash_buddy_ietb
    user: cash_buddy_ietb_user

services:
  - type: web
    name: Cash-Buddy
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cash_buddy_ietb
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
